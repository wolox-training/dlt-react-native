import { StyleSheet } from 'react-native';

import { mexicanRed } from '../../../../../constants/colors';

export default StyleSheet.create({
  todo: {
    marginTop: 50
  },
  deleteMultipleBtn: {
    color: mexicanRed,
    fontSize: 20,
    fontWeight: 'bold'
  }
});
